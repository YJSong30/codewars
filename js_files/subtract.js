/**
 * @description subtracts 2 numbers
 * @param {number} a number to subtract from
 * @param {number} b num to subtract from a
 * @returns {number} returns a - b
 */
function subtract(a, b) {
  return a - b;
}

module.exports = subtract;
